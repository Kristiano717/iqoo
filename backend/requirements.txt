fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
pydantic==2.10.4

# AI provider — Gemini free tier for local dev/testing (per CLAUDE.md).
# Swap for `openai` when moving to the GPT-5 default; keep the
# {summary, tasks, facts} prompt/response contract identical either way.
google-generativeai==0.8.3

# Storage — added when Milestone 2 ("save works") wires up persistence.
supabase==2.10.0
