fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
pydantic==2.10.4

# AI provider — Gemini free tier for local dev/testing (per CLAUDE.md).
# Both installed so swapping providers is purely an .env change (see
# backend/ai.py) — only one's key should actually be set at a time.
google-generativeai==0.8.3
# Separate, newer SDK — only it can mint the ephemeral tokens the browser
# needs to open a Gemini Live socket (see ai.py: create_live_token).
google-genai==2.22.0
openai==1.58.1

# Storage — added when Milestone 2 ("save works") wires up persistence.
# Upgraded from 2.10.0: google-genai requires httpx>=0.28.1 while the old
# supabase pinned httpx<0.28 — a real conflict, not a warning to ignore.
# It happened to keep working locally, but a fresh install (Render) could
# resolve it either way.
supabase==2.31.0
