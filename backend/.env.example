# Copy to .env and fill in. Never commit the real .env (see root .gitignore).

# AI provider — dev/testing uses the Gemini free tier per CLAUDE.md.
# Only ONE of these should be set; backend code checks which is present
# to decide the provider rather than assuming.
GEMINI_API_KEY=
# OPENAI_API_KEY=

# Supabase (wired up in Milestone 2 — "save works")
SUPABASE_URL=
SUPABASE_KEY=
